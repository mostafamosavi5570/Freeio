import './LoginButton.css';

export default function LoginButton() {
    return (
        <div className="LoginButton">
            <button className='button'>ثبت نام / ورود</button>

        </div>
    )
}